<local:LiveTileBase x:Class="Expenses.Wpf.LiveTileSmall"
             xmlns:local="clr-namespace:Expenses.Wpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="120" Height="120" x:Name="root">
    <Grid Background="{StaticResource Brush_F06060}">
        <TextBlock Style="{StaticResource LiveTileTextTiny}" Margin="15,16,0,86" Text="{Binding Label1, ElementName=root}"/>
        <TextBlock Style="{StaticResource LiveTileTextTiny}" Margin="15,35,0,66" Text="{Binding Label2, ElementName=root}"/>
        <TextBlock Style="{StaticResource LiveTileTextTiny}" Margin="15,56,0,46" Text="{Binding Amount, StringFormat=C0, ElementName=root}"/>
    </Grid>
</local:LiveTileBase>
